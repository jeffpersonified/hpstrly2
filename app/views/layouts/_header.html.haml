%nav#fixie
  %ul.inline-list
    %li= link_to( image_tag('logo_small.png'), root_path, class: 'logo')
    %li.pull-right= render 'devise/menu/registration_items'
    %li.pull-right= render 'devise/menu/login_items'
  - if notice
    %p.alert-box.secondary.radius
      = notice
      %a.close{:href => ""} &times;
  - if alert
    %p.alert-box.alert.radius
      = alert
      %a.close{:href => ""} &times;
